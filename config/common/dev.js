const host = '0.0.0.0';

const port = 10000;

const open = false;

// const templateDev = '@vuepress/client/templates/dev.html'

const devConfig = {
  host, // 指定开发服务器的主机名
  port, // 指定开发服务器的端口号
  open, // 是否在开发服务器启动后打开浏览器
  // templateDev // 指定开发时使用的 HTML 模板
};

export default devConfig;
